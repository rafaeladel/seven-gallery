<%= form_for [@gallery, @photo], html: { multipart: true } do |f| %>
    <div>
        <%= f.label :caption %>
        <%= f.text_field :caption %>
    </div>
    <div>
        <%= f.file_field :image %>
    </div>

    <%= f.submit %>
<% end %>